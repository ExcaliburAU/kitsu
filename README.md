# Conduit

Lean, themeable Matrix desktop client with a plugin host.

## Run

```bash
npm install
npm run desktop
```

Web-only: `npm start` → http://127.0.0.1:6080

## Package

```bash
npm run dist:win    # NSIS installer
npm run dist:linux  # AppImage (on Linux / CI)
```
